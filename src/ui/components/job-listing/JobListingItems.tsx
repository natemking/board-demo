import type { JobListingItemsProps } from 'types';

export function JobListingItems({ searchParams }: JobListingItemsProps): React.JSX.Element {
    console.log(searchParams);
    return (
        <div>
            Enter
        </div>
    );
}