import type { MarkdownRendererProps } from 'types';

export function MarkdownRenderer({className, options, ...props}: MarkdownRendererProps): React.JSX.Element {
    return (
        <div>
            Enter
        </div>
    );
}