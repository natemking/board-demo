import type { DaySincePostingProps } from 'types';

export async function DaysSincePosting({ postedAt}: DaySincePostingProps): Promise<React.JSX.Element> {
    return (
        <div>
            Enter
        </div>
    );
}